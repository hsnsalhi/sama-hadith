image: node:20-alpine

pages:
  stage: deploy
  script:
    - cd client && npm ci && npm run build
    - mv dist ../public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
